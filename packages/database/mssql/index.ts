// export * from './generated/client';
// import { PrismaClient } from './generated/client';

// export const mssqlPrisma = new PrismaClient({
//     log: process.env.NODE_ENV === 'development' ? ['query', 'error', 'warn'] : ['error'],
// });

export const mssqlPrisma = 111;
